# Magnet alignment

The neodymium magnet targets rotational recovery toward the steel strip. It must not fix an X position, create a slot, or impede normal sliding. Start with an air/plastic gap, test force with loaded garments, and move it away from the PN532 antenna. `REQUIRES_PHYSICAL_TEST`.
