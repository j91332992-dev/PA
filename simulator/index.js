'use strict';const base=process.env.BASE_URL||'http://localhost:8787',device=process.env.DEVICE_TOKEN||'development-device-token',count=Math.max(1,Number(process.argv[2]||50));let seq=0;const boot=Date.now().toString(16);function id(i){return`HC-${i.toString(16).padStart(6,'0').toUpperCase()}`}async function send(i,state){const r=await fetch(base+'/api/gateway/status',{method:'POST',headers:{authorization:'Bearer '+device,'content-type':'application/json'},body:JSON.stringify({gatewayId:'GW-SIM001',hangerId:id(i),state,tagUid:state==='PRESENT'?`04A1${i.toString(16).padStart(10,'0')}`:null,sequence:++seq,bootId:boot,channel:6,rssi:-35-Math.floor(Math.random()*40),firmwareVersion:'sim-1.0.0',gatewayFirmwareVersion:'sim-1.0.0'})});if(!r.ok)throw Error(await r.text())}Promise.all(Array.from({length:count},(_,i)=>send(i,Math.random()<.72?'PRESENT':'EMPTY'))).then(()=>console.log(`[SIM] ${count}개 상태 전송 완료 -> ${base}`)).catch(e=>{console.error(e);process.exit(1)});
